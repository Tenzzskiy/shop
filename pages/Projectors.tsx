import styles from './Projectors/Projectors.module.scss'
import Layout from "../components/Layout/Layout";
const Projectors = ({  }) => {
    return (
        <Layout >
Projectors
        </Layout>
    )
}

export default Projectors;